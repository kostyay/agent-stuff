{
  "id": "as-3946",
  "title": "Fix tests/status-bar.test.ts — update mocks for current API",
  "status": "open",
  "type": "task",
  "priority": 2,
  "tests_passed": false,
  "created_at": "2026-03-10T09:05:16.327Z",
  "parent": "as-2165"
}

Fix the broken `tests/status-bar.test.ts` test file. All 20 behavior tests are currently failing with `TypeError: Cannot read properties of undefined (reading 'on')` — the mock `ExtensionAPI` object is missing methods that `status-bar.ts` now calls during registration.

**File to fix:** `tests/status-bar.test.ts`
**Extension under test:** `pi-extensions/status-bar.ts`

**Root cause:** The extension calls `pi.events.on(...)` during initialization, and the mock doesn't provide a `pi.events` object. The pi extension API has evolved since these tests were written.

**Current test structure (736 lines):**
- Pure function tests (lines ~1-400): `hashString`, `hslToRgb`, `formatTokenCount`, `getProfileName`, `getAuthLabel`, `buildProfileBadge` — these likely still pass since they test exported pure functions
- E2E behavior tests (lines ~400-736): `statusBarExtension(mockPi)` — these all fail because the mock is incomplete

**How to fix:**

1. Read the current `pi-extensions/status-bar.ts` to identify every `pi.*` method called during `statusBarExtension(pi)` registration:
   - `pi.on(eventName, handler)` — event handlers for turn_start, turn_end, agent_end, session_start, session_switch, tool_execution_end, message events
   - `pi.events.on(...)` — listens for custom events (e.g. "ticket:stats")
   - `pi.registerFooter(component)` — registers the footer component
   - `pi.exec(cmd, args)` — runs git commands for branch info
   - Any other method calls

2. Update the mock `ExtensionAPI` to include all required methods. The mock needs to:
   - Capture registered event handlers so tests can invoke them
   - Capture the footer component so tests can call `render(width)` on it
   - Provide `pi.events` with an `on()` method
   - Provide `pi.exec()` that returns mock git output

3. Update assertions for any behavioral changes (new footer format, new fields, etc.).

**Approach:** The pure function tests should be left mostly untouched. Focus on fixing the mock construction for the E2E behavior tests. Read the extension's init function carefully to build a complete mock.

**Conventions:** Keep using `node:test` + `node:assert/strict`. Fix existing tests, don't rewrite from scratch.

## Acceptance Criteria

- All tests in `tests/status-bar.test.ts` pass (both pure function tests and E2E behavior tests)
- No tests removed — only updated to match current API
- Mock covers all `ExtensionAPI` methods used by `status-bar.ts` during registration

## Tests

- `node --experimental-strip-types --test tests/status-bar.test.ts` passes with 0 failures
- Existing tests still pass (no regressions in other test files)
