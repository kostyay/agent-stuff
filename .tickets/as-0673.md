{
  "id": "as-0673",
  "title": "Test infrastructure — helpers.ts",
  "status": "in_progress",
  "type": "task",
  "priority": 0,
  "tests_passed": false,
  "created_at": "2026-03-10T00:45:16.416Z",
  "parent": "as-5a68",
  "assignee": "9aa041ba-56ee-499b-8dd5-102c872d7a93"
}

## Types
Import `AgentConfig` from `../agents.js` for the factory functions. The `MockPi` type comes from `@marcfargas/pi-test-harness`.

## Acceptance Criteria

- File exists at `pi-extensions/subagent/test/helpers.ts`
- All helpers are exported and importable from test files
- `createMockPi()` works on macOS (Windows path optional but should not break)
- `writeTestAgents()` creates valid `.md` files parseable by `discoverAgents()`
- `events.*` builders produce valid JSONL event objects matching pi's JSON mode output format

## Tests

No direct tests — validated by all downstream test files importing and using these helpers successfully.
