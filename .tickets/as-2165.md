{
  "id": "as-2165",
  "title": "Extension test coverage",
  "status": "open",
  "type": "epic",
  "priority": 1,
  "tests_passed": false,
  "created_at": "2026-03-10T09:02:13.930Z"
}

Add comprehensive test coverage for pi extensions, targeting ~145 new tests across 7 new test files plus fixes for 2 broken test files.

Strategy:
- Unit tests for pure functions (parsing, formatting, state logic)
- Integration tests for filesystem-dependent logic (agent discovery)
- Fix existing broken tests caused by API drift
- Extract shared test helpers to avoid duplication

All tests use Node's built-in test runner (`node:test` + `node:assert/strict`), matching the existing convention in `tests/`. Run via `node --experimental-strip-types --test tests/*.test.ts`.

Current state: 171 tests passing across 4 files (ticket-core, changelog, control-channel, timed-confirm). 2 files broken (plan-ask, status-bar). Target: ~316 tests passing across 11 files.

## Acceptance Criteria

- All 9 sub-tasks completed and passing
- `npm test` runs all test files with zero failures
- No regressions in existing 171 passing tests
- Every new export has a JSDoc comment per AGENTS.md rules

## Tests

- `node --experimental-strip-types --test tests/*.test.ts` passes with 0 failures
- Total test count ≥ 300
