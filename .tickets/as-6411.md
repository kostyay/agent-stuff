{
  "id": "as-6411",
  "title": "Unit tests for tui-helpers.ts",
  "status": "open",
  "type": "task",
  "priority": 2,
  "tests_passed": false,
  "created_at": "2026-03-10T00:46:30.381Z",
  "parent": "as-5a68",
  "deps": [
    "as-0673"
  ]
}

Create `pi-extensions/subagent/test/tui-helpers.test.ts` — pure unit tests for exported functions in `pi-extensions/subagent/tui-helpers.ts`.

## Context
`tui-helpers.ts` provides TUI rendering primitives shared by agent-manager, log-viewer, and dashboard. Functions are mostly pure (some need a `Theme` mock for border rendering). Imports `visibleWidth` from `@mariozechner/pi-tui` and `AgentConfig` type from `./agents.js`.

## Functions to test

### `agentColor(displayIndex)`
- `AGENT_COLORS` has 9 entries: accent, syntaxString, syntaxFunction, warning, syntaxType, syntaxKeyword, success, syntaxNumber, mdLink
- Index 1 → `"accent"` (first color)
- Index 9 → `"mdLink"` (last color)
- Index 10 → wraps to `"accent"` (modulo)
- Index 0 → wraps to last color (edge case — `(0-1) % 9`)

### `fuzzyScore(query, text)`
- Exact substring match → score ≥ 100 (substring bonus)
- Character-by-character match → positive score, lower than substring
- No match → 0
- Empty query on non-empty text → test behavior
- Case insensitive — `"Scout"` matches `"scout"`
- Consecutive character bonus — `"abc"` in `"abcdef"` scores higher than `"abc"` in `"axbxcx"`

### `fuzzyFilterAgents(agents, query)`
- Empty query → returns all agents unchanged
- Query matching one agent name → returns just that agent
- Query matching description → returns matches (weighted 0.8)
- Query matching model → returns matches (weighted 0.6)
- No matches → returns empty array
- Results sorted by score descending

### `pad(s, len)`
- String shorter than len → padded with spaces
- String at len → unchanged
- String longer than len → no truncation (just returns as-is, `Math.max(0, ...)`)
- String with ANSI escapes → pads based on visible width (uses `visibleWidth`)

### `renderBorderRow(text, width, theme, style)`
- Header style → `╭` and `╮` corners
- Footer style → `╰` and `╯` corners
- Text centered within borders
- Header text uses `"accent"` color, footer uses `"dim"`
- Need a minimal Theme mock with `fg(color, text)` returning text wrapped in markers

### `borderedRow(content, width, theme)`
- Content padded to fill inner width (width - 2 for borders)
- `│` borders on each side
- Empty content → full padding between borders

## Theme mock
Create a simple mock: `{ fg: (color, text) => text, bold: (text) => text }` — enough for testing structural output. For tests that verify color application, can use `(color, text) => \`[\${color}]\${text}\`` pattern.

## Test framework
Use `node:test` + `node:assert/strict`. Import from `../tui-helpers.js`. Use `tryImport` if `@mariozechner/pi-tui` may not be available.

## Acceptance Criteria

- All 6 exported functions tested
- `fuzzyScore` and `fuzzyFilterAgents` cover ranking/sorting behavior
- `node --test test/tui-helpers.test.ts` passes

## Tests

`node --test pi-extensions/subagent/test/tui-helpers.test.ts` — all tests pass
